// 日本語コメント: 関数とクラスと定数
export const MAX_ITEMS = 10;

export const createStore = () => new Store();

export function newStore() {
  return new Store();
}

export class Store {
  add(item) {
    void item;
  }
}
